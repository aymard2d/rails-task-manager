<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task)%>
    </li>
  <% end %>
</ul>

<li> <%= link_to "Ajouter une nouvelle tâche", new_task_path(@task) %> </li>
